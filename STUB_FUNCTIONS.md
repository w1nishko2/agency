# 🎭 Система заглушек функций - Этап 2 (Верстка)

## 📋 Назначение

Система заглушек позволяет продемонстрировать заказчику полностью готовую верстку проекта **без реальной функциональности**. Все формы, кнопки и интерактивные элементы показывают красивые уведомления о том, что функция будет реализована на этапе 3.

---

## ✅ Что установлено

### Файлы системы:
- **CSS**: `public/css/stub-functions.css` - стили уведомлений
- **JS**: `public/js/stub-functions.js` - логика перехвата событий
- **Подключено в**:
  - `resources/views/layouts/main.blade.php` (публичная часть)
  - `resources/views/layouts/admin.blade.php` (админ-панель)

### Что работает автоматически:

✅ **Все формы** - перехватываются автоматически  
✅ **Демо-бейдж** - показывается в левом нижнем углу  
✅ **Первое уведомление** - показывается при первом посещении  

---

## 🎨 Как это выглядит

### Уведомления
Красивое модальное окно в правом верхнем углу с:
- Градиентным фоном (фиолетовый)
- Иконкой инструментов
- Заголовком "Этап 2: Верстка завершена"
- Сообщением о разработке функции
- Кнопкой закрытия
- Автоматическим исчезновением через 5 секунд

### Демо-бейдж
Маленький бейдж в левом нижнем углу:
- Для публичной части: "Демо-версия (Этап 2)"
- Для админки: "Админка: Демо (Этап 2)"

---

## 🛠 Как использовать

### 1. Автоматический перехват форм

**ВСЕ формы** на сайте автоматически перехватываются и показывают уведомление.

Система автоматически определяет тип формы:
- Кастинг → "Обработка заявок на кастинг будет реализована на этапе 3"
- Бронирование → "Система бронирования будет реализована на этапе 3"
- Авторизация → "Авторизация и регистрация будут реализованы на этапе 3"
- И т.д.

### 2. Добавить заглушку на кнопку вручную

```html
<!-- Вариант 1: Базовая заглушка -->
<button data-stub>Сохранить</button>

<!-- Вариант 2: С указанием типа -->
<button data-stub="save">Сохранить</button>
<button data-stub="delete">Удалить</button>
<button data-stub="upload">Загрузить фото</button>

<!-- Вариант 3: С кастомным сообщением -->
<button data-stub="custom" data-stub-message="Экспорт данных будет добавлен позже">
    Экспорт в Excel
</button>
```

### 3. Доступные типы заглушек

| Тип | Сообщение |
|-----|-----------|
| `default` | Данная функция будет реализована на этапе 3 |
| `form` | Отправка данных формы будет реализована на этапе 3 |
| `filter` | Фильтрация моделей будет реализована на этапе 3 |
| `search` | Поиск будет реализован на этапе 3 |
| `booking` | Система бронирования будет реализована на этапе 3 |
| `casting` | Обработка заявок на кастинг будет реализована на этапе 3 |
| `auth` | Авторизация и регистрация будут реализованы на этапе 3 |
| `profile` | Функции личного кабинета будут реализованы на этапе 3 |
| `admin` | Функции админ-панели будут реализованы на этапе 3 |
| `upload` | Загрузка файлов будет реализована на этапе 3 |
| `delete` | Удаление будет реализовано на этапе 3 |
| `edit` | Редактирование будет реализовано на этапе 3 |
| `save` | Сохранение данных будет реализовано на этапе 3 |

### 4. Отключить заглушку для конкретного элемента

Если нужно, чтобы какая-то форма или кнопка работала реально:

```html
<!-- Для формы -->
<form action="/real-action" method="POST" class="stub-allow">
    <!-- эта форма будет работать -->
</form>

<!-- Для кнопки -->
<button class="stub-allow">Эта кнопка работает</button>
<a href="/page" class="stub-allow">Эта ссылка работает</a>
```

### 5. Вызвать уведомление из JavaScript

```javascript
// Простое уведомление
window.showStubNotification('Функция в разработке');

// С указанием типа
window.showStubNotification(window.stubMessages.upload, 'info');
```

---

## 📦 Примеры использования

### Пример 1: Форма кастинга
```html
<form id="casting-form" action="{{ route('casting.store') }}" method="POST">
    @csrf
    <!-- поля формы -->
    <button type="submit">Отправить заявку</button>
</form>
```
✅ Автоматически перехватывается, показывает сообщение о кастинге

### Пример 2: Кнопка удаления в админке
```html
<button data-stub="delete" class="btn btn-danger">
    <i class="bi bi-trash"></i> Удалить
</button>
```
✅ Показывает: "Удаление будет реализовано на этапе 3"

### Пример 3: Фильтры моделей
```html
<select name="category" data-filter-stub>
    <option value="">Все категории</option>
    <option value="women">Девушки</option>
</select>
```
✅ При изменении показывает сообщение о фильтрах

### Пример 4: Поиск
```html
<input type="search" name="search" data-search-stub placeholder="Поиск...">
```
✅ При нажатии Enter показывает сообщение о поиске

---

## 🎭 Отключение системы заглушек

### Для этапа 3 (когда функции готовы)

**Вариант 1**: Удалить подключения из layouts:

```blade
<!-- Удалить из main.blade.php и admin.blade.php -->
<link rel="stylesheet" href="{{ asset('css/stub-functions.css') }}">
<script src="{{ asset('js/stub-functions.js') }}"></script>
<div class="stub-demo-badge">...</div>
```

**Вариант 2**: Закомментировать в layouts:

```blade
{{-- 
<link rel="stylesheet" href="{{ asset('css/stub-functions.css') }}">
<script src="{{ asset('js/stub-functions.js') }}"></script>
--}}
```

**Вариант 3**: Удалить файлы:
- `public/css/stub-functions.css`
- `public/js/stub-functions.js`

---

## 🎨 Настройка внешнего вида

### Изменить цвет уведомлений

В `public/css/stub-functions.css`:

```css
/* Синий градиент */
.stub-notification-content {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}

/* Красный градиент */
.stub-notification-content {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

/* Зеленый градиент */
.stub-notification-content {
    background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
}
```

### Убрать демо-бейдж

Просто удалите из layouts:

```blade
<div class="stub-demo-badge">
    <i class="bi bi-tools"></i>
    Демо-версия (Этап 2)
</div>
```

### Отключить первое уведомление

В `public/js/stub-functions.js` закомментируйте:

```javascript
// const isFirstVisit = !sessionStorage.getItem('stub-notification-shown');
// if (isFirstVisit) {
//     setTimeout(() => {
//         showStubNotification(...);
//     }, 1000);
// }
```

---

## 📱 Адаптивность

Система полностью адаптивна:
- На мобильных уведомления занимают всю ширину (с отступами 10px)
- Демо-бейдж становится меньше на мобильных
- Все анимации работают плавно

---

## ⚡ Производительность

- Легковесная система (< 10KB CSS + JS)
- Нет зависимостей от внешних библиотек
- Не влияет на скорость загрузки страницы
- Работает на чистом JavaScript (ES6)

---

## 🐛 Известные ограничения

1. **AJAX-формы** - не перехватываются автоматически (нужно добавлять вручную)
2. **SPA-приложения** - требуют дополнительной настройки
3. **Динамически создаваемые элементы** - могут не перехватываться (используйте `data-stub`)

---

## 📞 Часто задаваемые вопросы

### Как добавить новый тип сообщения?

В `public/js/stub-functions.js`:

```javascript
const messages = {
    // ...существующие...
    newType: 'Ваше новое сообщение'
};
```

Использование:
```html
<button data-stub="newType">Кнопка</button>
```

### Как изменить время показа уведомления?

В `public/js/stub-functions.js`:

```javascript
setTimeout(() => {
    // ...
}, 5000); // ← изменить на нужное количество миллисекунд
```

### Как показать уведомление при загрузке страницы?

```javascript
document.addEventListener('DOMContentLoaded', function() {
    window.showStubNotification('Добро пожаловать в демо-версию!', 'info');
});
```

---

## ✨ Готово!

Теперь ваш проект готов для демонстрации заказчику на **Этапе 2**.  
Вся верстка работает, все формы и кнопки показывают красивые уведомления о разработке.

При переходе к **Этапу 3** просто отключите систему заглушек и добавьте реальную функциональность.
