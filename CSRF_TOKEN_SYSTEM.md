# Система автоматического обновления CSRF токена

## Описание проблемы
Ошибка **419 Page Expired** возникает, когда CSRF токен Laravel истекает (обычно через 2 часа), а пользователь не перезагружал страницу.

## Решение
Реализована система автоматического обновления CSRF токена, которая работает на всех страницах сайта.

## Как это работает

### 1. API Endpoint
**Файл:** `routes/api.php`

Создан endpoint для получения свежего токена:
```php
Route::get('/api/csrf-token', function () {
    return response()->json([
        'token' => csrf_token(),
        'timestamp' => time()
    ]);
});
```

### 2. CSRF Token Manager
**Файл:** `resources/js/csrf-token-manager.js`

Автоматический менеджер токенов с функциями:

#### ✅ Периодическое обновление
- Автоматически обновляет токен каждые **90 минут** (до истечения сессии)
- Предотвращает ошибку 419 для пользователей, держащих вкладку открытой долгое время

#### ✅ Обновление при возврате на вкладку
- Проверяет и обновляет токен, когда пользователь возвращается на неактивную вкладку

#### ✅ Автоматическая повторная отправка при 419
- Перехватывает все fetch и axios запросы
- При получении ошибки 419:
  1. Автоматически запрашивает новый токен
  2. Обновляет его в DOM и заголовках
  3. Повторяет неудачный запрос с новым токеном

#### ✅ Предотвращение дублирования запросов
- Если несколько запросов получили 419 одновременно, токен обновляется только один раз
- Все запросы ждут завершения обновления и повторяются автоматически

## Интеграция

### Автоматическая интеграция
Система автоматически активируется на всех страницах через `bootstrap.js`:

```javascript
import './csrf-token-manager';
```

### Для fetch запросов
```javascript
// Токен добавляется автоматически
fetch('/api/endpoint', {
    method: 'POST',
    body: JSON.stringify(data)
});
```

### Для axios запросов
```javascript
// Токен добавляется автоматически
axios.post('/api/endpoint', data);
```

### Принудительное обновление
Если нужно принудительно обновить токен:

```javascript
// Глобальный доступ к менеджеру
if (window.csrfTokenManager) {
    await window.csrfTokenManager.forceRefresh();
}
```

## Требования

### Layouts
Все layouts должны содержать meta-тег с CSRF токеном:

```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```

✅ Уже настроено в:
- `resources/views/layouts/main.blade.php`
- `resources/views/layouts/app.blade.php`
- `resources/views/layouts/admin.blade.php`

### Vite/Laravel Mix
Система подключается автоматически через:

```php
@vite(['resources/css/app.css', 'resources/js/app.js'])
```

## Логи и отладка

Система выводит информационные сообщения в консоль браузера:

- `✅ CSRF Token Manager инициализирован` - успешный запуск
- `⏰ Автообновление токена каждые 90 минут` - настройка периодического обновления
- `🔄 CSRF токен обновлен` - токен успешно обновлен
- `⚠️ Ошибка 419: CSRF токен истек, обновляем...` - перехват ошибки и автоматическое восстановление
- `👀 Вкладка активна, проверяем токен...` - проверка при возврате на вкладку

## Настройки

Для изменения интервала обновления токена отредактируйте `csrf-token-manager.js`:

```javascript
// По умолчанию: 90 минут (1.5 часа)
this.refreshInterval = 90 * 60 * 1000;

// Примеры других значений:
// 60 минут: 60 * 60 * 1000
// 30 минут: 30 * 60 * 1000
// 2 часа: 120 * 60 * 1000
```

## Преимущества

✅ **Автоматическая работа** - не требует изменений в существующем коде  
✅ **Прозрачность** - перехватывает все HTTP запросы  
✅ **Надежность** - автоматически восстанавливается после ошибок  
✅ **Производительность** - минимизирует количество запросов на обновление  
✅ **UX** - пользователи могут держать вкладку открытой неделями без проблем  

## Проверка работы

1. Откройте консоль браузера (F12)
2. Перезагрузите страницу
3. Убедитесь, что видите: `✅ CSRF Token Manager инициализирован`
4. Подождите или вручную вызовите обновление
5. Проверьте, что токен обновляется без ошибок

## Совместимость

- ✅ Laravel 8.x, 9.x, 10.x, 11.x
- ✅ Все современные браузеры (Chrome, Firefox, Safari, Edge)
- ✅ Fetch API и Axios
- ✅ SPA и традиционные multi-page приложения

## Безопасность

Система не снижает безопасность приложения:
- CSRF токены остаются защищенными
- Каждый токен генерируется Laravel и привязан к сессии
- Обновление токена не требует аутентификации
- Все запросы остаются защищенными от CSRF атак

---

**Дата создания:** 13 февраля 2026  
**Статус:** ✅ Активно и работает на всех страницах
