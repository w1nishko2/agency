# 🚀 БЫСТРЫЙ СТАРТ - Golden Models

## ✅ Все уже готово!

Проект полностью настроен и готов к запуску.

---

## 📋 Что уже сделано

✅ **Frontend**
- Минималистичный дизайн (референс: abamodels.com)
- Bootstrap 5.3.3
- Чистый CSS (без Sass)
- Адаптивная верстка
- JavaScript компоненты

✅ **Страницы**
- Главная
- Каталог моделей с фильтрами
- Карточка модели
- Кастинг (12-шаговая форма)
- О компании
- Блог
- Контакты
- Личный кабинет модели
- Админ панель

✅ **Backend**
- 5 контроллеров
- 5 моделей Eloquent
- 4 миграции БД
- Middleware для админа
- Seeder с тестовыми данными

---

## ⚡ Запуск за 5 минут

### 1. Настройка окружения (ОБЯЗАТЕЛЬНО!)

```powershell
# Скопировать файл окружения
Copy-Item .env.example .env

# Отредактировать .env - установить имя БД
# DB_DATABASE=golden_models
```

### 2. Создать базу данных

Откройте phpMyAdmin (OSPanel) и создайте БД:

```sql
CREATE DATABASE golden_models CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. Установка зависимостей

```powershell
# Backend
composer install

# Frontend
npm install
```

### 4. Генерация ключа

```powershell
php artisan key:generate
```

### 5. Миграции и тестовые данные

```powershell
# Создать таблицы
php artisan migrate

# Загрузить тестовые данные (5 моделей, 5 статей блога)
php artisan db:seed --class=TestDataSeeder

# Создать символическую ссылку для загрузок
php artisan storage:link
```

### 6. Сборка frontend

```powershell
# Для разработки
npm run dev

# ИЛИ для продакшена
npm run build
```

### 7. Запуск сервера

```powershell
php artisan serve
```

Сайт доступен: **http://localhost:8000**

---

## 🔑 Тестовые учётные данные

После запуска seeder доступны:

**Администратор:**
- Email: `admin@goldenmodels.ru`
- Пароль: `password`
- Доступ: `/admin/dashboard`

**Модель:**
- Email: `anna@example.com`
- Пароль: `password`
- Доступ: `/profile`

---

## 📄 Структура проекта

### Основные страницы:
- `/` - Главная
- `/models` - Каталог моделей
- `/model/{id}` - Карточка модели
- `/casting` - Форма кастинга
- `/about` - О компании
- `/blog` - Блог
- `/contacts` - Контакты
- `/profile` - Личный кабинет (требует авторизации)
- `/admin/dashboard` - Админ панель (требует admin права)

### Тестовые данные включают:
- 5 моделей (4 женщины, 1 мужчина)
- 5 категорий блога
- 5 статей блога
- 1 администратор

---

## 🛠 Полезные команды

### Очистка кеша
```powershell
php artisan cache:clear
php artisan config:clear
php artisan view:clear
php artisan route:clear
```

### Пересоздать БД с данными
```powershell
php artisan migrate:fresh --seed --seeder=TestDataSeeder
```

### Запуск в фоне (watch mode)
```powershell
# В одном терминале
php artisan serve

# В другом терминале
npm run dev
```

---

## 📚 Документация

Полная документация находится в файлах:

- `README_PROJECT.md` - Описание проекта
- `DEPLOYMENT.md` - Подробная инструкция по развертыванию
- `API_DOCUMENTATION.md` - Описание всех маршрутов и API

---

## 🐛 Возможные проблемы

### Ошибка "Class not found"
```powershell
composer dump-autoload
```

### Ошибка прав доступа
```powershell
icacls "storage" /grant Everyone:F /t
icacls "bootstrap/cache" /grant Everyone:F /t
```

### Vite не компилирует assets
```powershell
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm install
npm run build
```

### База данных не подключается
Проверьте в `.env`:
- DB_DATABASE должна совпадать с созданной БД
- DB_USERNAME и DB_PASSWORD корректны
- MySQL сервер запущен в OSPanel

---

## ✨ Что дальше?

После успешного запуска вы можете:

1. **Войти как админ** (`admin@goldenmodels.ru`) и проверить админ панель
2. **Войти как модель** (`anna@example.com`) и проверить личный кабинет
3. **Протестировать форму кастинга** на `/casting`
4. **Посмотреть каталог моделей** с фильтрами
5. **Почитать блог** с тестовыми статьями

---

## 🎯 Основные фичи

- ✅ Адаптивный дизайн (mobile-first)
- ✅ Фильтры для каталога моделей
- ✅ 12-шаговая форма кастинга с валидацией
- ✅ Загрузка и управление фотографиями
- ✅ Система бронирования моделей
- ✅ Блог с категориями и поиском
- ✅ Личный кабинет для моделей
- ✅ Админ панель для управления
- ✅ Bootstrap 5.3.3
- ✅ Минималистичный монохромный дизайн

---

## 📞 Контакты

Проект создан для **Golden Models Agency**

При возникновении проблем проверьте:
1. Логи Laravel: `storage/logs/laravel.log`
2. Консоль браузера (F12)
3. Версии PHP (≥8.1), Composer, Node.js (≥16)

---

**Успешного запуска! 🎉**
