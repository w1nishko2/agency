# Система Inline-редактирования контента

## ⚠️ ВАЖНО: Как работает редактор

### Что можно редактировать
- **Заголовки** (h1, h2, h3 и т.д.)
- **Параграфы** (простой текст в `<p>`)
- **Отдельные текстовые элементы** на странице

### Что НЕ редактируется
- **HTML-контент из поля `description`** - это богатый HTML с разметкой, который редактируется через админку
- Навигация, футер, кнопки
- Служебные элементы

## Как это работает

### Два типа контента:

#### 1. HTML из поля `description` (редактируется в админке)
```blade
{!! $page->description !!}
```
Это полноценный HTML с заголовками, списками, параграфами. Редактируется в админ-панели через текстовое поле или редактор.

#### 2. Отдельные текстовые элементы (редактируются inline)
```html
<h1>Заголовок</h1>
<p>Простой текст</p>
```
Эти элементы можно кликнуть и отредактировать прямо на странице. Изменения сохраняются в `content_map`.

## Установка

### 1. Запустить миграцию
```bash
php artisan migrate
```

### 2. Скомпилировать ресурсы
```bash
npm run build
```

## Как использовать

### Шаг 1: Открыть страницу в режиме редактирования
1. Зайдите в админку: `/admin/pages/1/edit` (или любую другую страницу)
2. Нажмите кнопку **"Редактировать текст на странице"**
3. Откроется публичная страница с панелью редактирования сверху

### Шаг 2: Редактировать текст
1. **Наведите курсор** на любой текст (заголовок, параграф, ссылку и т.д.)
2. Текст подсветится синей рамкой
3. **Кликните** на текст для начала редактирования
4. Измените текст как вам нужно
5. Нажмите **"Сохранить"** или `Ctrl+Enter`

### Шаг 3: Выйти из режима редактирования
Нажмите кнопку **"Выйти"** в верхней панели

## Возможности

### ✨ Что можно редактировать
- Заголовки (h1, h2, h3, h4, h5, h6)
- Параграфы (p)
- Текст в любых элементах (span, div, a, li и т.д.)
- Весь видимый текст на странице

### 🎨 Визуальные особенности
- Подсветка при наведении курсора
- Текст редактируется в том же виде, как на странице (не HTML)
- Панель инструментов сверху
- Уведомления об успехе/ошибке

### ⌨️ Горячие клавиши
- `Ctrl+Enter` - Сохранить изменения
- `Esc` - Отменить редактирование

### 🔒 Безопасность
- Доступ только для администраторов (role = 'admin')
- CSRF защита всех запросов
- Автоматическая проверка прав доступа

## Технические детали

### Как это работает
1. При входе в режим редактирования JavaScript автоматически находит все текстовые элементы
2. Каждому элементу присваивается уникальный ID (если его нет)
3. При сохранении изменения записываются в поле `content_map` таблицы `pages`
4. При загрузке страницы сохраненный контент автоматически применяется

### Структура хранения
Изменения сохраняются в JSON-формате в поле `content_map`:
```json
{
  "editable-h1-0": "Новый заголовок",
  "editable-p-5": "Новый текст параграфа"
}
```

### Маршруты
- `GET /pages/{slug}/inline-edit` - Открыть страницу в режиме редактирования
- `POST /api/inline-edit/update` - Сохранить изменения
- `GET /api/inline-edit/content/{pageId}` - Получить сохраненный контент

## Troubleshooting

### Проблема: 403 "Доступ запрещен"
**Решение:** Убедитесь, что вы авторизованы и имеете role = 'admin'

### Проблема: Не сохраняются изменения
**Решение:** 
1. Проверьте, что миграция выполнена
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что в таблице `pages` есть поле `content_map`

### Проблема: Текст не подсвечивается при наведении
**Решение:** 
1. Убедитесь, что выполнена команда `npm run build`
2. Очистите кеш браузера (Ctrl+F5)
3. Проверьте, что файлы CSS и JS подключены

## Разработка

### Добавление новых страниц
Чтобы добавить поддержку inline-редактирования для новой страницы:

1. Добавьте `data-page-id` в контейнер:
```blade
@if($page)
<div data-page-id="{{ $page->id }}">
    <!-- Контент страницы -->
</div>
@endif
```

2. Добавьте маршрут в контроллере `InlineEditController`

### Исключение элементов из редактирования
Элементы внутри этих областей не будут редактируемыми:
- `nav` - навигация
- `footer` - подвал
- `button`, `input`, `textarea` - формы
- Элементы панели редактирования

## Авторы
Разработано для модельного агентства Golden Models
